export interface IAlert {
    color: string;
    message: string;
    visible: boolean;
}

export interface Registration {
    id: number;
    name: string;
    participation: boolean;
    timestamp: string;
}